#include <stdio.h>
/**
 * main- a container for the program
 * Return: 0 if the program runs else returns non zero
 */
int main(void)
{
	printf("with proper grammar,but the outcome is a piece of art,
	\n");
	return (0);
}
