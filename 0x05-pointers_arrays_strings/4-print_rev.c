#include "main.h"
/**
 * print_rev - function that prints a string, in reverse,
 * followed by a new line
 * @s: the input string
 * Return: always 0
 */
void print_rev(char *s)
{
	while (*s != '\0')

